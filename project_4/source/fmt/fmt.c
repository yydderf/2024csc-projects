#include <stdint.h>
#include <stdio.h>

int main(){
  setvbuf(stdout, 0, 2, 0);
  setvbuf(stdin, 0, 2, 0);
  
  char flag[40] = "FLAG{??????????????????????????????????}";
  char input[24];
  
  scanf("%s", input);
  printf(input);

  return 0;
}